let horas = 45;
let ex = (horas - 40);

if (horas>40) {
    console.log (`o limite de horas foi excedido em ${ex} horas`)
}

else {
    console.log (`o funcionario não exedeu o limite de horas`)
}